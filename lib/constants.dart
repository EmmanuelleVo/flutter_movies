import 'package:flutter/material.dart';

const mainBackgroundColor = Color(0xFFeaeaea);

const fontStyleMenuItem = TextStyle(fontWeight: FontWeight.w600, fontSize: 16);
const fontStyleLegend = TextStyle(fontSize: 14, color: Colors.grey);
const homeGradientButton = LinearGradient(
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
  colors: [
    Color(0xFF0093e9),
    Color(0xFF80DBC9),
  ],
);
